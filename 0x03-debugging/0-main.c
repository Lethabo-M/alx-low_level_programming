#include "main.h"
#include <stdlib.h>
/**
* main - Test function for positive or negative
* Return:Always 0
*/

int main(void)
{
	int i;

	if (i == 0)
		printf("%d is zero", i);

	printf("\n");
	return (0);
}
